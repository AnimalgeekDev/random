kind: pipeline
name: default

steps:
- name: test-docker
  image: docker:dind
  volumes:
  - name: docker_sock
    path: /var/run/docker.sock
  commands:
  - docker build -t latest

services:
  docker:
    image: docker:dind
    privileged: true
    volumes:
    - name: dockersock
      path: /var/run

volumes:
  - name: docker_sock
    host:
      path: /var/run/docker.sock